// This file is autogenerated by the hyperschema compiler
// Schema Version: 1
/* eslint-disable camelcase */
/* eslint-disable quotes */
/* eslint-disable space-before-function-paren */

import { c } from 'hyperschema/runtime'

const VERSION = 1

// eslint-disable-next-line no-unused-vars
let version = VERSION

// @example/request1
const encoding0 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.field1) c.uint.preencode(state, m.field1)
    if (m.field2) c.string.preencode(state, m.field2)
  },
  encode(state, m) {
    const flags =
      (m.field1 ? 1 : 0) |
      (m.field2 ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.field1) c.uint.encode(state, m.field1)
    if (m.field2) c.string.encode(state, m.field2)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      field1: (flags & 1) !== 0 ? c.uint.decode(state) : 0,
      field2: (flags & 2) !== 0 ? c.string.decode(state) : null
    }
  }
}

// @example/request2
const encoding1 = {
  preencode(state, m) {
    state.end++ // max flag is 2 so always one byte

    if (m.field1) c.string.preencode(state, m.field1)
    if (m.field2) c.uint.preencode(state, m.field2)
  },
  encode(state, m) {
    const flags =
      (m.field1 ? 1 : 0) |
      (m.field2 ? 2 : 0)

    c.uint.encode(state, flags)

    if (m.field1) c.string.encode(state, m.field1)
    if (m.field2) c.uint.encode(state, m.field2)
  },
  decode(state) {
    const flags = c.uint.decode(state)

    return {
      field1: (flags & 1) !== 0 ? c.string.decode(state) : null,
      field2: (flags & 2) !== 0 ? c.uint.decode(state) : 0
    }
  }
}

function setVersion(v) {
  version = v
}

function encode(name, value, v = VERSION) {
  version = v
  return c.encode(getEncoding(name), value)
}

function decode(name, buffer, v = VERSION) {
  version = v
  return c.decode(getEncoding(name), buffer)
}

function getEnum(name) {
  switch (name) {
    default:
      throw new Error('Enum not found ' + name)
  }
}

function getEncoding(name) {
  switch (name) {
    case '@example/request1':
      return encoding0
    case '@example/request2':
      return encoding1
    default:
      throw new Error('Encoder not found ' + name)
  }
}

function getStruct(name, v = VERSION) {
  const enc = getEncoding(name)
  return {
    preencode(state, m) {
      version = v
      enc.preencode(state, m)
    },
    encode(state, m) {
      version = v
      enc.encode(state, m)
    },
    decode(state) {
      version = v
      return enc.decode(state)
    }
  }
}

const resolveStruct = getStruct // compat

export { resolveStruct, getStruct, getEnum, getEncoding, encode, decode, setVersion, version }
